import { {{pascalCase name}}Entity } from '@/{{dashCase name}}/domain/entities/{{dashCase name}}.entity';
import { {{pascalCase name}}Mapper } from '@/{{dashCase name}}/application/dtos/{{dashCase name}}-output';

describe('{{pascalCase name}} output unit tests', () => {
  it('should convert a {{lowerCase name}} in output', () => {
  });
});
